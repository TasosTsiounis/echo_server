RFC 862 is a low-level TCP/UDP protocol

- TCP server

For that we use node's net module whiich gives direct control of TCP sockets. 

We can test either by running the tcp_client.js either by tools like telnet or ncat

- UDP server

used node's dgram

